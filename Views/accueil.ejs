<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-Airbnb</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="nav-container">
            <a href="/" class="logo">Mini-Airbnb</a>
            <div class="search-bar">
                <input type="text" placeholder="Où allez-vous ?">
                <input type="date">
                <input type="date">
                <button>🔍</button>
            </div>
            <div class="user-menu">
                <div class="user-profile">
                    <span>☰</span>
                    <span>👤</span>
                </div>
            </div>
        </div>
    </header>

    <main class="main-container">
        <div class="filters-bar">
            <div class="filter-chip active">Tous</div>
            <div class="filter-chip">Maisons</div>
            <div class="filter-chip">Appartements</div>
            <div class="filter-chip">Villas</div>
        </div>

        <div class="listings-grid">
            <% logements.forEach(logement => { %>
                <a href="/logement/<%= logement.id %>" class="listing-card">
                    <div class="listing-image-container">
                        <img src="<%= logement.image %>" alt="<%= logement.titre %>" class="listing-image">
                        <button class="favorite-btn">♡</button>
                    </div>
                    <div class="listing-info">
                        <div class="rating">
                            <span class="rating-star">★</span>
                            <span class="rating-score">4.8</span>
                        </div>
                        <div class="listing-location"><%= logement.lieu %></div>
                        <div class="listing-details"><%= logement.type || 'Logement entier' %></div>
                        <div class="listing-price">
                            <span class="price-amount"><%= logement.prix %> €</span>
                            <span class="price-period">par nuit</span>
                        </div>
                    </div>
                </a>
            <% }) %>
        </div>
    </main>

    <footer class="footer">
        <div class="footer-container">
            <p class="footer-text">© 2025 Mini-Airbnb. Tous droits réservés.</p>
        </div>
    </footer>
</body>
</html>